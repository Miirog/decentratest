import type { Request } from 'express';
import { Strategy } from 'passport-strategy';
import { Options } from './types';
export declare class DecentralandStrategy extends Strategy {
    private options;
    name: string;
    constructor(options?: Options);
    authenticate(req: Request, options: Options): void;
}
